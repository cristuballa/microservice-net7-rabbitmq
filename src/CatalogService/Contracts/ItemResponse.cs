 namespace Contracts;
  public record ItemResponse (
                    Guid Id,
                      string Description,
                      string Name,
                      float Price
                      );
